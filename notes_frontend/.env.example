# Frontend environment variables (React)
#
# By default the frontend will call the backend at http://localhost:3001.
# If you deploy backend elsewhere, override it here:
REACT_APP_API_BASE_URL=http://localhost:3001
